import 'package:flutter/material.dart';

class AddEditEmployee extends StatelessWidget {
  static String routeName() => '/add_edit_employee';

  const AddEditEmployee({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
